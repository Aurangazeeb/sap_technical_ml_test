apiVersion: v1
kind: Service
metadata:
  name: product-similarity
  labels:
    app: product-similarity
spec:
  selector:
    app: product-similarity
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
